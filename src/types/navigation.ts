/** Route parameter types for Expo Router typed routes. */

export type ChatRouteParams = {
  sessionId: string;
};

export type IntakeMode = "symptom_logging" | "triage_submission";

export type TabRoute = "index" | "care" | "routines" | "profile";
